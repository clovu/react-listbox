name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: sxzz/workflows/.github/workflows/release.yml@v2
    with:
      build: pnpm build:lib
      publish: true
    permissions:
      contents: write
      id-token: write
